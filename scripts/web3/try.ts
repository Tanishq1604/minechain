// app.ts
// const { Server, Keypair, TransactionBuilder, BASE_FEE, Networks } = require('soroban-sdk');
import * as SorobanClient from "soroban-client";



// amount input -> author info ->










// Define a function that will be triggered on button click
function handleClick() {
    alert("Button clicked!");
  }


// Add event listener to the button
document.getElementById("marketButton")?.addEventListener("click", handleClick);
 